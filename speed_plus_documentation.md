# SPEED+ 数据集集成文档

## 概述

SPEED+是一个航天器姿态估计数据集，包含航天器的11个3D关键点和对应的6DOF姿态信息。本文档详细说明了数据结构、关键点定义、相机参数以及在DiffPose框架中的集成方法。SPEED+包含69,491张总图像。合成部分包含59,960张计算机生成的图像，使用斯坦福大学基于OpenGL的光学刺激器创建，其中80%用于训练，20%用于验证。低光照和强曝光图像包含9,531张真实图像，可用于测试验证。

## 1. 航天器关键点定义

### 1.1 关键点坐标表

SPEED+航天器包含11个特征关键点，其3D坐标（米）如下：

| 关键点ID | X坐标 | Y坐标 | Z坐标 | 描述 |
|---------|-------|-------|-------|------|
| 0 | -0.37 | -0.265 | 0.02 | 航天器左下角特征点 |
| 1 | -0.37 | 0.305 | 0.02 | 航天器左上角特征点 |
| 2 | 0.37 | 0.305 | 0.02 | 航天器右上角特征点 |
| 3 | 0.37 | -0.265 | 0.02 | 航天器右下角特征点 |
| 4 | -0.37 | 0.385 | 0.32 | 左侧高度特征点 |
| 5 | 0.37 | 0.385 | 0.32 | 右侧高度特征点 |
| 6 | 0.37 | -0.385 | 0.32 | 右后侧特征点 |
| 7 | -0.37 | -0.385 | 0.3 | 左后侧特征点 |
| 8 | 0.3 | -0.585 | 0.247 | 前端中央特征点 |
| 9 | -0.54 | 0.485 | 0.247 | 左翼端点 |
| 10 | 0.55 | 0.495 | 0.247 | 右翼端点 |

### 1.2 关键点拓扑结构

航天器关键点的连接关系（用于可视化和结构约束）：

```
基础矩形框架: 0-1-2-3-0
高度框架: 4-5-6-7-4  
垂直连接: 0-7, 1-4, 2-5, 3-6
翼部特征: 1-9, 2-10
前端特征: 3-8, 6-8
```

### 1.3 坐标系定义

- **坐标系**：右手坐标系
- **X轴**：航天器宽度方向（左负右正）
- **Y轴**：航天器长度方向（后负前正）
- **Z轴**：航天器高度方向（下负上正）
- **单位**：米（m）
- **原点**：航天器几何中心

## 2. 相机参数配置

### 2.1 基本参数

| 参数名称 | 数值 | 单位 | 描述 |
|---------|------|------|------|
| 图像宽度 (Nu) | 1920 | 像素 | 图像水平分辨率 |
| 图像高度 (Nv) | 1200 | 像素 | 图像垂直分辨率 |
| 像素尺寸 X (ppx) | 5.86E-6 | 米 | 像素物理尺寸X方向 |
| 像素尺寸 Y (ppy) | 5.86E-6 | 米 | 像素物理尺寸Y方向 |
| 焦距 X (fx) | 0.0175 | 米 | 物理焦距X方向 |
| 焦距 Y (fy) | 0.0175 | 米 | 物理焦距Y方向 |

### 2.2 相机内参矩阵

```
K = [[2988.58, 0,       960 ],
     [0,       2988.34, 600 ],
     [0,       0,       1   ]]
```

- **fx, fy**: 2988.58, 2988.34 (像素焦距)
- **cx, cy**: 960, 600 (主点坐标)

### 2.3 畸变系数

```
D = [-0.2238, 0.5141, -0.0007, -0.0002, -0.1312]
```

- **k1, k2, k3**: -0.2238, 0.5141, -0.1312 (径向畸变)
- **p1, p2**: -0.0007, -0.0002 (切向畸变)

## 3. 数据格式说明

### 3.1 输入数据格式

**姿态数据 (JSON格式)**:
```json
{
  "filename": "img000001.jpg",
  "r_Vo2To_vbs_true": [-0.129896, 0.069519, 6.457073],
  "q_vbs2tango_true": [-0.336458, -0.093409, -0.8286, 0.437599]
}
```

- `r_Vo2To_vbs_true`: 3D平移向量 [x, y, z] (米)
- `q_vbs2tango_true`: 四元数旋转 [qx, qy, qz, qw]

**3D关键点模板 (Excel格式)**:
```
   P0    P1    P2    ... P10
X -0.37 -0.37  0.37 ... 0.55
Y -0.265 0.305 0.305 ... 0.495  
Z  0.02  0.02  0.02 ... 0.247
```

### 3.2 处理后数据格式

**DiffPose兼容格式**:
- **3D姿态**: `(N, 11, 3)` - N个样本，每个11个关键点，每个关键点3个坐标
- **2D投影**: `(N, 11, 2)` - N个样本，每个11个关键点，每个关键点2个图像坐标
- **相机参数**: `[fx_norm, fy_norm, cx_norm, cy_norm, k1, k2, p1, p2, k3]`

## 4. 坐标变换流程

### 4.1 3D关键点变换

1. **加载关键点模板**: 从Excel读取11个标准3D坐标
2. **应用旋转**: `P_rotated = R @ P_template.T`
3. **应用平移**: `P_transformed = P_rotated + t`

其中：
- `R`: 四元数转换的旋转矩阵 (3×3)
- `t`: 平移向量 (3×1)
- `P_template`: 标准关键点坐标 (11×3)

### 4.2 2D投影计算

使用OpenCV的PNP算法：
```python
points_2d, _ = cv2.projectPoints(
    points_3d_transformed,  # 变换后的3D点
    rvec,                   # 旋转向量
    tvec,                   # 平移向量  
    camera_matrix,          # 相机内参
    dist_coeffs            # 畸变系数
)
```

### 4.3 坐标归一化

类似Human3.6M的处理方式：
```python
# 归一化到[-1, 1]范围，保持纵横比
normalized_coords = coords_2d / w * 2 - [1, h / w]
```

